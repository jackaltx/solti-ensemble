---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart openclaw
  ansible.builtin.systemd:
    name: openclaw.service
    state: restarted
  failed_when: false
